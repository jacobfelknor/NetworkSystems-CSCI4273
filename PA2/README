to build the project:
    make

remove binaries:
    make clean

binary stored in ./bin/server

Usage: 
    ./bin/server <port>

Assumptions:
    assuming that there is a directory ./www/ in the cwd wherever the server is being ran
    so, if running ./bin/server from the root of this project, there must exist a ./www/