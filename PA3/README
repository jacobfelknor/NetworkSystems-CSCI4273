to build the project:
    make

remove binaries:
    make clean

binary stored in ./bin/proxy

Usage: 
    ./bin/server <port>

Assumptions:
    assuming a ./cache folder already exists in the current directory.
    
    TODO: blocklist